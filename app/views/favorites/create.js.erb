$(".favorite").html("<%= escape_javascript(render partial: 'favorites/favorite', locals: {post: @post}) %>");
// インスタンス変数は、favorites_controllerのcreateアクションで定義されたものが使用できます。
// 以下の記述でも同じ挙動をします。html()以降では部分テンプレートを呼び出しています。
$(".favorite").html("<%= j(render 'favorites/favorite', post: @post) %>");