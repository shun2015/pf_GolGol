<h4>プロフィール</h4>
<%= attachment_image_tag user, :profile_image, :fill, 100, 100, format: 'jpeg', fallback: "no_image.jpg" %><br />
名前：<%= user.name %><br />
性別：<%= user.gender %><br />
住所：<%= user.address %><br />
年齢：<%= user.age %>代<br />
平均スコア：
<% if user.average_score.present? %>
  <%= user.average_score %>代
<% else %>
  登録していません
<% end %><br />
紹介文<br /><div class="word-break"><%= user.introduction %></div><br />
<% if user.id == current_user.id %>
  <p><%= link_to "プロフィール編集", edit_user_path(user) %></p>
<% end %><br />

<% if current_user != user %>
  <%= link_to 'chatを始める', chat_path(user.id)%>
<% end %><br />

フォロー:
<%= link_to following_user_path(@user) do %>
  <%= @user.following.count %>
<% end %>
フォロワー:
<%= link_to followers_user_path(@user) do %>
  <%= @user.followers.count %>
<% end %><br>
      
<% unless @user == current_user %>
  <% if current_user.following?(@user) %>
    <%= link_to 'フォロー外す', user_relationships_path(@user), method: :DELETE %>
  <% else %>
    <%= link_to 'フォローする', user_relationships_path(@user), method: :POST %>
  <% end %>
<% end %>